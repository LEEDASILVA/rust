fn main() {
    let a = 45 // by default it is i32 -> signed 32 bit integer
    let b: i64 = 23421234253646 // now this is a i64
    let c: u64 = 2342564 // if we now that the values will not be negative we just use an unsigned integer
    let d = 4.3 // float -> f32
    let e: f64 = 4.3 // float -> f64
    let ver: bool = false
    // ......
}